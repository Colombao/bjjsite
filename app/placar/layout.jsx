import './placar-shell.css';

export const metadata = {
  title: 'Placar — CT Heishikan',
  description: 'Placar de luta ao vivo — CT Heishikan',
};

export default function PlacarLayout({ children }) {
  return children;
}

import './placar-shell.css';

export const metadata = {
  title: 'Placar — CT Heishikan Aurum',
  description: 'Placar de luta ao vivo — CT Heishikan Aurum',
};

export default function PlacarLayout({ children }) {
  return children;
}
